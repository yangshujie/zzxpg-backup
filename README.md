# zzxpg backup

This repository stores the current backend and algorithm service projects:

- `pgzzx/` - backend service code
- `zgpg_algs/` - algorithm service code

Generated build output, local virtual environments, logs, IDE files, caches, and local environment files are excluded from Git.
